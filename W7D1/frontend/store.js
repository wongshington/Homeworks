import { createStore } from 'redux';
import reducer from './reducer.js';

//should keep track of app's state
  //our AJAX request should update store with results
    //what success callback in out Widget's ajax call is trying to do

  const Store = createStore(reducer);





  export default Store;
